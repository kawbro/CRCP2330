/* Needs to read a file of assembly commands.
Input of commands outputs new Binary instruction. -> Returns int/String of binary
Inputs a .asm file; Outputs a .hack file
.hack file = binary code (16 bits aka "0" or "1" per line)
.asm file = Instruction (A:@ command or C:D/M/A command) and Symbols
*/
